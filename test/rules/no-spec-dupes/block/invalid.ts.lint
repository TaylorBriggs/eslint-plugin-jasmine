it('Same spec name', function(): void {});
it('Same spec name', function(): void {});
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ [Duplicate spec: "Same spec name."]

describe('Parent context', () => {
  describe('Same parent', () => {
    it('Same spec', function(): void {});
  });
  describe('Same parent', () => {
    it('Same spec', function(): void {});
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ [Duplicate spec: "Same spec."]
  });
});
