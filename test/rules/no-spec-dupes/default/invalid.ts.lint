it('Same spec name', function(): void {});
it('Same spec name', function(): void {});
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ [Duplicate spec: "Same spec name."]

it('Handling' + ' string ' + 'concatenation', function(): void {});
it('Handling string concatenation', function(): void {});
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ [Duplicate spec: "Handling string concatenation."]
