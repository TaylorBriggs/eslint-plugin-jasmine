describe('The first suite name', () => {});
describe('The second suite name', () => {});

describe('unique', () => {
  it('spec', function(): void {});
});
describe('different', () => {
  it('spec', function(): void {});
});

describe('context', () => {
  describe('unique', () => {
    it('spec', function(): void {});
  });
});
describe('different', () => {
  it('spec', function(): void {});
});

describe('same', () => {
  describe('same', () => {
    it('spec', function(): void {});
  });
});
describe('same', () => {
  describe('same', () => {
    it('different', function(): void {});
  });
  describe('same', () => {
    it('unique', function(): void {});
  });
});
