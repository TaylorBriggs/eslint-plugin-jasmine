var result = someFunc()
result = someFunc()
const other = createSpy('onSuccess')
const another = createSpy('onSuccess')
let otherSpy = library.createSpy('onSuccess');
otherSpy = library.createSpy('onSuccess')
let onSuccess = jasmine.createSpy('onSuccess');
onSuccess = jasmine.createSpy('onSuccess');
foo(jasmine.createSpy('callback'))

someObject = {
  someFunc: jasmine.createSpy('someFunc')
}

someObj = {
  someFunc: jasmine.createSpy('someFunc').and.callThrough()
};

function someFunc() {
  this.spy = jasmine.createSpy('spy').and.callThrough();
}

let spy = jasmine.createSpy();
          ~~~~~~~~~~~~~~~~~ [unnamed-spy]

spy = jasmine.createSpy();
      ~~~~~~~~~~~~~~~~~ [unnamed-spy]

var spy = jasmine.createSpy('callback');
          ~~~~~~~~~~~~~~~~~ [wrong-name]

spy = jasmine.createSpy('callback')
      ~~~~~~~~~~~~~~~~~ [wrong-name]

someObj = {
  spy: jasmine.createSpy('someFunc')
       ~~~~~~~~~~~~~~~~~ [wrong-name]
};

someObj = {
  spy: jasmine.createSpy('someFunc').and.callThrough()
       ~~~~~~~~~~~~~~~~~ [wrong-name]
};

function someFunc() {
  this.spy = jasmine.createSpy('someFunc').and.callThrough();
             ~~~~~~~~~~~~~~~~~ [wrong-name]
}

[unnamed-spy]: Unnamed spy.
[wrong-name]: Variable should be named after the spy name.
