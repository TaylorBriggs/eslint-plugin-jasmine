describe('', () => {
  beforeEach(function(): void {});
});

const helper = (): void => {
  beforeEach(function: void () {});
};

describe('suite', () => {
  helper();

  it('', function(): void {});
});

describe.only('suite', () => {
  beforeAll(function(): void {
    let a: number = 1;
  });
});

describe['only']('suite', () => {
  beforeAll(function(): void {
    const a: number = 1;
  });
});
